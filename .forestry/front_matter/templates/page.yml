---
label: Page
hide_body: true
fields:
- name: page_title
  type: text
  config:
    required: false
  label: Page title
- name: banner_image
  type: file
  config:
    maxSize: 64
  label: Banner image
- name: lead_text
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: Lead text
- name: content_blocks
  type: blocks
  label: Content blocks
  template_types:
  - card
  config:
    min: 
    max: 
